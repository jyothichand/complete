pipeline {
    agent any

    triggers {
        pollSCM '* * * * *'
    }
    stages {
    stage('list') {
            steps {
                bat 'dir'
            }
        }
        stage('Build') {
            steps {
                sh './gradlew build'
            }
        }
        stage('Test') {
            steps {
                sh './gradlew test'
            }
        }
    }
}
